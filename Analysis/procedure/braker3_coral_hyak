#open the apptainer
apptainer shell braker3.sif
#each run needs
#a_cytherea
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=a_cytherea_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add a_cytherea to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g a_cytherea_soft_mask_breaker.gtf -o a_cytherea_soft_mask_breaker_filtered.gtf
#a_hyactintus
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=a_hyactintus_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add a_hyactintus to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g a_hyactintus_soft_mask_braker.gtf -o a_hyactintus_soft_mask_braker_filtered.gtf
#d_cribrosa
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=d_cribrosa_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add d_cribrosa to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g d_cribrosa_soft_mask_braker.gtf -o d_cribrosa_soft_mask_braker_filtered.gtf
#fungia
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=fungia_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add fungia to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g fungia_soft_mask_braker.gtf -o fungia_soft_mask_braker_filtered.gtf
#g_aspera
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=g_aspera_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add g_aspera to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g g_aspera_soft_mask_braker.gtf -o g_aspera_soft_mask_braker_filtered.gtf 
#g_fascicularis
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=g_fascicularis_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add g_fascicularis to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g g_fascicularis_soft_mask_braker.gtf -o g_fascicularis_soft_mask_braker_filtered.gtf 
#m_capitata
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=m_capitata_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add m_capitata to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g m_capitata_soft_mask_braker.gtf -o m_capitata_soft_mask_braker_filtered.gtf
#o_faveolata
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=o_faveolata_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add o_faveolata to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g o_faveolata_soft_mask_braker.gtf -o o_faveolata_soft_mask_braker_filtered.gtf
#p_damicornis
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=p_damicornis_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add p_damicornis to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g p_damicornis_soft_mask_braker.gtf -o p_damicornis_soft_mask_braker_filtered.gtf
#p_lutea
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=p_lutea_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add p_lutea to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g p_lutea_soft_mask_braker.gtf -o p_lutea_soft_mask_braker_filtered.gtf 
#p_rus
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=p_rus_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add p_rus to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g p_rus_soft_mask_braker.gtf -o p_rus_soft_mask_braker_filtered.gtf
#p_sinensis
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=p_sinensis_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add p_sinensis to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g p_sinensis_soft_mask_braker.gtf -o p_sinensis_soft_mask_braker_filtered.gtf 
#p_speciosa
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=p_speciosa_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add p_speciosa to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g p_speciosa_soft_mask_braker.gtf -o p_speciosa_soft_mask_braker_filtered.gtf 
#p_verrucosa
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=p_verrucosa_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#copy out the breaker.aa, breaker.codingseq, and breaker.gtf and add p_verrucosa to the biginning
#zip the output
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g p_verrucosa_soft_mask_braker.gtf -o p_verrucosa_soft_mask_braker_filtered.gtf
#s_pistillata
braker.pl --AUGUSTUS_CONFIG_PATH=/mmfs1/gscratch/zaneveld/Augustus_config --genome=s_pistillata_full_mask_soft.fasta --prot_seq=../Metazoa.fa --threads=20
#need to only get the longest open reading frames as some of the outputs have multiple similar isoforms
get_longest_isoform.py -g braker/braker.gtf -o s_pistillata_soft_mask_braker_filtered.gtf

